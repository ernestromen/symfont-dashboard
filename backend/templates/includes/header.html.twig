<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">My Symfony App</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
			<span class="navbar-toggler-icon"></span>
		</button>
		{% if is_granted('IS_IMPERSONATOR') %}

			<div class="btn btn-primary ">
				<a class="text-decoration-none text-light" href="{{ impersonation_exit_path(path('app_home')) }}">Exit impersonation</a>
			</div>
		{% endif %}

		<div class="collapsenavbar-collapse" id="navbarNav">
			<ul class="navbar-nav ms-auto">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('users') }}">
							users
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('roles') }}">
							roles
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('permissions') }}">
							permissions
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('products') }}">
							products
						</a>
					</li>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('categories') }}">
						categories
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title={{app.user.userIdentifier}}>
						<i class="fa-solid fa-user ml-3"></i>

						<span>Hello
							{{ app.user.userIdentifier }}</span>
					</a>
				</li>

				<li class="nav-item">
					<a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="/login">Login</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="/register">Register</a>
				</li>
			{% endif %}
		</ul>
	</div>
</div></nav>
